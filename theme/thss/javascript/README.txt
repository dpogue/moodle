Make edits to the uncompressed .js file, then use Uglify JS to compress it
for production.
Moodle concatenates all these JavaScript files into a single theme file,
but compression is still useful to make that single file smaller.

Unglify JS: http://marijnhaverbeke.nl/uglifyjs
